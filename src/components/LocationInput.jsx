import React from 'react'
import { connect } from 'react-redux'

const LocationInput = ({ locations, currentLocation }) => {

  return (
    <div>
      Location Input
    </div>
  )
}

export default connect()(LocationInput)